\section{引言}

\subsection{周期通信局部释放聚集的形成机制与相位配置}

车载通信系统中的控制量、状态量与诊断状态往往按照预先配置的周期重复发送。对于周期报文而言，周期决定相邻两次释放之间的时间间隔，Offset 则决定该周期序列相对于共同时间原点的初始相位。当多条报文具有相同或相近的 Offset，或者其周期关系使若干释放序列在公共倍数处反复相交时，原本分散在不同报文上的工作量会在局部时间窗口内聚集。以 CAN 或 CAN FD 为例，物理总线在同一时刻只能完成一帧传输；多个发送请求的同时出现不会增加总线的并行服务能力，而会形成仲裁、排队与短时需求峰值。因而，即使网络的长期平均工作量处于可接受范围，局部释放聚集仍可能放大瞬时竞争，并使仅依据平均负载作出的判断掩盖时间分布上的不均衡。

经典 CAN schedulability analysis 以优先级仲裁、阻塞与干扰为基础分析报文最坏响应时间\cite{tindell1994}；在此基础上，显式考虑 Offset 的分析与配置研究表明，对周期流进行去同步化能够改变干扰结构，并可在高负载条件下收紧响应时间估计或改善调度表现\cite{grenier2008,yomsi2012}。这些工作说明，相位配置不是无关紧要的实现细节，而是周期通信设计中的有效自由度。然而，在工程配置中，Offset 往往只能从给定的有限栅格中选取，报文的帧长和传输时间也并不相同。此时，“把报文错开”并不能充分刻画问题：需要明确局部窗口的划分方式、不同释放事件的权重、启动阶段与稳态阶段的区别，以及峰值约束和整体均衡性之间的优先关系，才能把直观的去同步要求转化为可计算、可审计的离散优化问题。

Offset 对长期平均释放工作量与局部分布的影响还必须严格区分。对于周期和单次释放权重固定的报文，改变 Offset 只平移释放序列，并不改变单位时间内的释放次数；在完整周期窗口或无限时域意义下，总工作量仍由各报文的周期与权重决定。因此，Offset 优化不能降低周期集合的长期平均释放工作量，也不能替代对平均负载不可行性的处理。其作用在于重塑工作量在时间轴上的局部分布，使峰值、超限程度与负载离散度在给定候选集合内得到改善。本文所研究的正是这一“总量守恒而局部分布可调”的组合优化问题。相应地，离散时隙负载是释放聚集程度的模型量，而不是完整 CAN 仲裁过程、端到端响应时间或实际总线利用率的同义表达。

\subsection{Offset 均衡与软件周期调度代价的耦合机制}

若只从总线时间轴观察，Offset 容易被理解为近乎“免费”的相位变量：只要周期、标识符和帧长不变，似乎可以在候选集合内任意移动释放序列，以换取更均匀的 CAN 侧分布。该理解忽略了周期报文通常还要由 ECU 软件中的周期入口组织和触发。以 AUTOSAR COM 为例，规范给出了 \texttt{Com\_MainFunctionTx} 及其 \texttt{ComMainTxTimeBase} 等配置概念，但内部 timing handling 允许存在实现相关性\cite{autosarcom}。当项目采用共同整数时间原点和精确 tick 对齐的周期调度模型时，一条周期为 $T_i$、Offset 为 $O_i$ 的报文若由基本节拍 $B$ 的周期入口精确表示，需要满足 $B\mid T_i$ 且 $B\mid O_i$。于是，该报文可支持的最大基本节拍为
\begin{equation}
 D_i=\gcd(T_i,O_i).
\label{eq:intro-di}
\end{equation}
式\eqref{eq:intro-di}揭示了本文主线中的关键耦合：Offset 不仅决定周期报文在总线时间轴上的释放相位，还会改变软件侧周期调度能够采用的时间粒度。某个有利于错开 CAN 释放的 Offset，可能使 $D_i$ 变小，迫使相关周期入口以更细的 TimeBase 被调用；反之，具有较大公约数的 Offset 可能有利于软件调度组织，却未必产生最佳的总线侧均衡。

上述整除关系是本文为配置建议建立的工程模型，而不是 AUTOSAR 规范对所有实现施加的唯一约束。在这一模型内，给定一个 MainFunction 分组后，其最大共同 TimeBase 由组内 $D_i$ 的最大公约数确定，因而 TimeBase 无需再作为独立外层变量搜索。软件侧代价也不能在缺少目标 ECU 执行时间测量时直接称为 CPU utilization。本文以“每次调用的固定开销加组内报文的线性增量开销”为结构假设，构造
\begin{equation}
 P_{\rm CPU}=\sum_g\frac{\rho+N_g}{B_g},
\label{eq:intro-cpu-proxy}
\end{equation}
其中 $N_g$ 和 $B_g$ 分别表示组内报文数与共同 TimeBase，$\rho$ 表示固定调用开销相对于单报文增量开销的比值。式\eqref{eq:intro-cpu-proxy}仅是比较不同分组结构的 CPU Cost Proxy，不是实际 CPU 占用率或百分比；其工程解释依赖于 $\rho$ 的选取，并有待目标平台 profiling 标定。

由此，Offset 分配形成两个相互关联而又不能相互替代的目标：总线侧希望降低稳态离散时隙负载的聚集程度，软件侧希望在精确表示释放相位的前提下避免过细、过多的周期调用。若将 Offset、MainFunction 归属和 TimeBase 同时放入一个外层组合搜索，变量之间的笛卡尔积会迅速扩大，也会掩盖子问题自身的结构。本文采用分层处理：对每个固定 Offset assignment，以确定性 subset dynamic programming 精确求解式\eqref{eq:intro-cpu-proxy}下的 MainFunction partition；在外层，则继续以 GCLS 及其锚点、预算扫描和 refinement 搜索 Offset。这里的“exact”严格限定于固定 Offset 下的内层分组子问题，外层 Offset 搜索仍为 heuristic，二者不能混称为联合问题的全局精确求解。

在多目标层面，本文以 Peak 条件作为 CAN 侧 guardrail，并采用 $\varepsilon$-constraint 将 $P_{\rm CPU}$ 作为预算约束，在不同预算下搜索较小的稳态负载平方和。所有搜索阶段发现的候选被汇入累计 archive，再在 CAN 均衡指标与 CPU Cost Proxy 两个维度上进行非支配过滤。因此，所得集合是有限搜索过程支持下的 observed Pareto front，而非全局 Pareto frontier。几何 knee 只对最终 observed front 进行解释性后处理，用于给出一个边际代价变化较为明显的折中候选；它不参与搜索，也不构成唯一工程最优的证明。这样的限定使总线均衡、软件调度代价和推荐机制处于同一因果链中，同时避免把代理量、启发式搜索结果或几何解释外推为超出证据范围的结论。

\subsection{研究方法与主要贡献}

围绕“周期释放聚集如何在离散 Offset 约束下得到缓解，以及这种相位调整将付出何种软件调度代价”这一问题，本文建立从单目标均衡到联合折中的分层方法。主要贡献概括如下。

\begin{enumerate}
  \item 建立面向周期报文释放聚集的离散时隙加权模型，将局部 Peak、物理阈值违反和稳态负载平方和置于统一评价框架，并明确证明 Offset 不改变长期平均释放工作量、只改变其局部分布。该模型解决了平均工作量与局部聚集容易混淆的问题；其结论限定于给定周期、候选 Offset、评价窗口和权重模型，不等同于完整 CAN schedulability 或端到端时延证明。

  \item 提出以 greedy construction、精确增量 1-opt、conflict-directed pair search 和受控 restart 为核心的 GCLS，用于在有限 Offset 组合空间中求取高质量均衡解。该方法针对直接枚举难以扩展而单次贪心又容易受早期决策限制的问题，保留可复现的比较规则与审计信息；但 GCLS 及其外层候选搜索属于 heuristic，除非另有穷举或精确求解证书，本文不据此宣称 Offset assignment 全局最优。

  \item 建立 Offset 与 MainFunction 调度粒度之间的最大公约数模型，并在固定 Offset 下将软件侧分组化为 $D_i$ 类型集合上的确定性 subset-DP。该分层利用相同 $D_i$ 类型可整体处理以及分组 TimeBase 由公约数唯一确定的结构，解决了同时搜索 Offset、分组和 TimeBase 所带来的组合膨胀；在式\eqref{eq:intro-cpu-proxy}及共同整数时间栅格等假设下，MainFunction partition 子问题是 exact，相关枚举对照用于核验实现一致性，但该精确性不扩展到外层 Offset 搜索，也不把 CPU Cost Proxy 转化为真实 CPU utilization。

  \item 构造带 Peak guardrail 的 CAN--CPU $\varepsilon$-constraint 联合搜索，并以累计 archive、iterative refinement、二维非支配过滤和几何 knee 形成从候选发现到折中解释的闭环。该方法避免用缺少量纲解释的固定加权和掩盖两类目标，并允许显式观察不同 CPU 预算下的 CAN 均衡代价；其输出严格称为 observed Pareto front，锚点、refinement 与前沿覆盖均受外层 heuristic 和搜索预算限制，knee 仅是条件化、可解释的推荐候选而非唯一工程最优。
\end{enumerate}

本文不在引言中预先使用尚未完成的九网段最终数值支撑上述贡献。后文将通过真实网段实验、内层精确求解器的独立枚举对照以及 seed、$\varepsilon$ 网格、$\rho$ 和 refinement 状态的敏感性分析，分别检验模型、算法与观察性结论；数值结果的外推范围以对应实验输入和搜索配置为限。

\subsection{相关研究}

\subsubsection{CAN 可调度性分析、Offset 分配与流量整形}

CAN 实时性研究的基础问题，是在固定优先级非抢占传输、阻塞和高优先级干扰作用下给出报文最坏响应时间上界。Tindell 等的分析框架奠定了经典 CAN response-time analysis 的基本形式\cite{tindell1994}。该类方法回答的是“给定报文参数和仲裁关系后是否满足时限”，与本文关注局部释放聚集具有直接联系，因为多个周期流的同步释放会形成保守但关键的干扰场景；但 schedulability analysis 的主要输出是响应时间或可调度性判据，并不等同于在有限 Offset 栅格上寻找加权释放分布较均匀的配置。

Offset-aware analysis 和 traffic shaping 工作进一步把相位信息纳入 CAN 研究。Grenier 等讨论了通过 Offset 去同步周期流以改善最坏响应时间的配置价值\cite{grenier2008}；Yomsi 等则在含 Offset 的事务模型上扩展响应时间分析，使相位关系能够进入最坏情况推导\cite{yomsi2012}。这些研究与本文共享“同步释放并非不可改变”的基本认识，也为把 Offset 视为设计变量提供了依据。其重点通常落在响应时间分析、干扰界或 Offset 带来的性能增益，而本文进一步关心离散候选、异质释放权重、局部 Peak guardrail 与整体平方和均衡之间的可计算关系，并把 Offset 的软件调度代价纳入同一模型。因此，本文方法不是对 CAN schedulability analysis 的替代，而是面向配置生成的前置 traffic/load shaping：它给出候选相位及其局部释放分布，完整的响应时间和仲裁安全性仍需由相应分析或外部验证承担。

\subsubsection{AUTOSAR COM 定时机制与周期调度组织}

AUTOSAR COM 规范描述了 \texttt{Com\_MainFunctionTx}、\texttt{ComMainTxTimeBase} 以及通信定时处理所需的配置语义\cite{autosarcom}，为周期发送在 ECU 软件侧的组织提供了标准化接口。另一方面，周期任务研究通常以执行成本与调用周期之比刻画处理器需求\cite{liu1973}，这一基本形式说明：当等价功能需要更高调用频率时，固定调度开销会被更频繁地支付。二者共同构成本文 CPU Cost Proxy 的建模背景，但规范本身并不为给定一组报文自动求解 Offset、MainFunction partition 与 TimeBase 的联合配置，实际 timing handling 也可能随生成器和平台实现而变化。

现有工程实践往往分别完成通信矩阵的 Offset 配置和 ECU 周期入口组织，因而容易把总线相位与软件调度粒度视为两个串行、彼此独立的步骤。本文的定位是在明确的共同整数时间栅格和 exact tick alignment 假设下，揭示二者通过 $\gcd(T_i,O_i)$ 发生的结构耦合，并对固定 Offset 下的分组给出精确组合求解。该模型用于生成可解释的配置建议，不声称是 AUTOSAR 标准规定的唯一调度方式，也不从配置结构直接推断实际 ECU CPU utilization。关于不同 AUTOSAR COM generator 的周期任务合并策略、实测调用开销模型及硬件平台 WCET 标定，仍需补充更有针对性的公开研究或厂商资料（\textbf{TODO：补充 COM MainFunction 周期组织与执行开销测量文献}）。

\subsubsection{多目标启发式优化、Epsilon-constraint 与 Pareto 分析}

离散 Offset assignment 具有有限但快速增长的组合空间，工程规模下常需借助贪心构造、局部搜索和多次 restart 等 heuristic 在可接受时间内寻找候选。此类方法能够利用问题结构并形成 anytime 式改进过程，但若没有可采纳下界或完整枚举，其结果只能被解释为已搜索空间中的高质量解。本文的 GCLS 属于这一范畴；固定 Offset 下的 exact MainFunction 子求解器改善了每个候选的评价精度，却不会自动赋予外层搜索全局最优性。

对于 CAN 均衡与 CPU Cost Proxy 这两个量纲和尺度不同的目标，固定线性加权和需要预先给定难以解释的权重，并可能遗漏离散非凸前沿上的部分解。$\varepsilon$-constraint 方法保留一个目标作为优化量、把另一个目标转化为显式预算，是多目标数学规划中常用的后验解集生成思路\cite{mavrotas2009}。本文沿用这一思想，但每个预算下调用的是启发式 Offset 搜索，而非对完整联合空间的精确数学规划求解。因此，预算网格、初始锚点、搜索顺序与 refinement 共同决定候选覆盖范围，非支配过滤只能保证 archive 内的集合性质，输出必须称为 observed Pareto front。

Pareto 方法保留不可相互支配的候选，把偏好选择推迟到搜索之后；knee point 则常被用来识别一个目标继续改善时另一目标代价开始加速上升的区域\cite{debgupta2011}。本文在最终 observed front 的归一化几何上采用 chord-based knee score，并为直线型前沿、点数不足等退化情况保留显式处理。其作用是减少工程人员浏览候选集的成本，而不是把偏好判断隐藏为新的优化目标。由于前沿会随 seed、$\varepsilon$ 分辨率、$\rho$ 和 refinement 预算变化，knee 也只能作为附带搜索条件的解释性折中推荐。面向车载周期通信的多目标 Offset 配置、尤其是同时涉及总线释放均衡与 COM 周期入口组织的公开工作仍需继续补充检索（\textbf{TODO：补充车载通信多目标 Offset/任务组织联合优化文献}）。
